/**
 * Re-export useInternetIdentity from @caffeineai/core-infrastructure.
 * Keeps backward-compatible import path for existing components.
 */
export { useInternetIdentity } from "@caffeineai/core-infrastructure";
